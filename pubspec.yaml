name: estagiobloc
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.5 <3.0.0'


ependencies:
  flutter:
    sdk: flutter

  bloc: ^8.1.1
  flutter_bloc: ^8.1.2
  cupertino_icons: ^1.0.5
  dio: ^5.1.1
  path_provider: ^2.0.14
  google_fonts: ^4.0.3
  permission_handler: ^10.2.0
  open_file_safe: any



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - lib/utils/assets/rocket.png
    - lib/utils/assets/nuvens.png
    - lib/utils/assets/morty.gif
    - lib/utils/assets/metroid.gif
    - lib/utils/assets/ashoka.gif
    - lib/utils/assets/megaman2.gif
    - lib/utils/assets/darth.gif
    - lib/utils/assets/goku.gif
    - lib/utils/assets/jedi.gif
    - lib/utils/assets/mario.gif
    - lib/utils/assets/rogue.gif
    - lib/utils/assets/tale2.gif
    - lib/utils/assets/thor2.gif
    - lib/utils/assets/venom.gif
    - lib/utils/assets/zero2.gif
    - lib/utils/assets/storm.gif
    - lib/utils/assets/spiderman.gif
    - lib/utils/assets/sonic2.gif
    - lib/utils/assets/mario2.gif
    - lib/utils/assets/ryu.gif
    - lib/utils/assets/tale2.gif
    - lib/utils/assets/pokebola.gif
    - lib/utils/assets/start.gif
    - lib/utils/assets/botao.gif
    - lib/utils/assets/titulo.gif
    - lib/utils/assets/botao03.gif
    - lib/utils/assets/botao04.gif
    - lib/utils/assets/botao05.gif
    - lib/utils/assets/titulo.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
